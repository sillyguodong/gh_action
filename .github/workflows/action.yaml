name: Test Gitea Repository Mirror

on:
  push:
    branches:
      - main
      - "test/*"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print something
        run: echo "Hello Gitea!"
      - name: Print something else
        run: echo "Hello Gitea again!!..."
      - name: Print bye
        run: echo "Bye Gitea! :)"
      - name: Print t2
        run: echo "hello branch test/t2..."
